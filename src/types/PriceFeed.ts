export interface DecodedPriceFeed {
    feedId: string
    price: string
    confidence: string
    exponent: number
    publishTime: string
    prevPublishTime: string
    emaPrice: string
    emaConfidence: string
  }